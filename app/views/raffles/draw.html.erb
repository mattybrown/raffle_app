	<h1>Draw the last raffle</h1>
		<%= form_tag(:controller => "raffles", :action => "draw") do %>
			<%= submit_tag :draw, :class => "btn btn-primary btn-large" %>
		<% end %>
	<div>The winner for the last drawn raffle is, <%= @winner %></div>
